# TODO

タスクは**上から順に**一つずつ対応し、再確認後に問題なければ `[x]` にする。




## 実装

- [ ] パッケージ `handy_calendar`（`python -m handy_calendar`）の骨格（`python-dotenv` で `.env` を読む）
- [ ] iCal 取得・解析（`ICAL_URLS` 全件成功時のみ続行）
- [ ] PNG 生成（今日・明日・[AGENTS.md](./AGENTS.md) の並び・決定性）
- [ ] Dot Image API 送信（公式ドキュメント準拠）
- [ ] PNG の解像度・レイアウトをデバイス要件に合わせて固定
