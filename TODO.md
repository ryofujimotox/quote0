# TODO

タスクは**上から順に**一つずつ対応し、再確認後に問題なければ `[x]` にする。




## 実装

※ すべての実装タスクで単体テストを同時実装する（実装の追加・変更と同じタイミングでテストを追加・更新する）

- [ ] iCal 取得処理を実装する（`ICAL_URLS` の列挙順で全件取得し、1件でも失敗なら失敗扱い）
- [ ] iCal 解析処理を実装する（`DTSTART`/`DTEND`/`TZID` を JST に正規化し、今日・明日の対象予定を抽出）
- [ ] 並び順ロジックを実装する（URL列挙順 → 開始時刻昇順 → `UID` 昇順の決定的ソート）
- [ ] PNG 描画処理を実装する（1枚に今日・明日の2枠、予定なし日も日付枠を表示）
- [ ] Dot Image API 送信処理を実装する（公式パラメータ準拠、成功時のみ更新）
- [ ] テストデータと最小テストを整備する（取得失敗時停止、日付抽出、並び順、描画結果の再現性）
- [ ] ドキュメント追記を行う（`README.md` は地図、手順は `docs/deploy.md`、仕様は `AGENTS.md` へのリンク整理）
